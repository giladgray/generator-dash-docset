language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-dash-docset' ]; then cd .. && eval "mv $currentfolder generator-dash-docset" && cd generator-dash-docset; fi
